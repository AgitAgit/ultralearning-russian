GENERAL
Data modeling:
Need to separate words into languages. 
(
    for each user an array of languages, each with an array of words?
    Or, one large array of words and each user has arrays of references?
)

having long arrays in a mongo doc is an anti pattern

CLIENT
Should use the normal navigation approach with react router